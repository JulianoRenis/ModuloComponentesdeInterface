<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.ModuloComponentesDeInterface" parent="Theme.Material3.Light">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="Theme.ModuloComponentesDeInterface" parent="Base.Theme.ModuloComponentesDeInterface" />

    <style name="buttonItemProdutoStyle" >
        <item name="android:background">@color/amarelo</item>
        <item name="android:tintMode">@color/amarelo</item>
        <item name="android:tint">@color/amarelo</item>
        <item name="android:radius">10dp</item>
       <item name="android:shape">rectangle</item>
        <item name="tint">@color/amarelo</item>
    </style>


</resources>